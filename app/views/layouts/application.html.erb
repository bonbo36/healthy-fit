<!DOCTYPE html>
<html>
  <head>
    <title>Healthyfit</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to "healthyfit", root_path %>
          </div>
          <%# ログインしているときは以下を表示するようにしましょう %>
          <%# <% if user_signed_in? %> 
             <div class="nav__right">
               <%= link_to "ログアウト", "#", class: :nav__logout, method: :delete %>
               <%= link_to "食事の登録", "#", class: :nav__btn %>
             </div> 
          <%# // ログインしているときは上記を表示するようにしましょう %>
          <%# ログインしていないときは以下を表示するようにしましょう %>
          <%# <% else %> 
            <div class="nav__right">
              <%= link_to "ログイン", "#", class: :nav__btn %>
              <%= link_to "新規登録", "#", class: :nav__btn %>
            </div>
          <%# <% end %> 
          <%# // ログインしていないときは上記を表示するようにしましょう %>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer>
      <p>
        Copyright Healthyfit 2023.
      </p>
    </footer>
  </body>
</html>
